#include "permissions.h"

int permissions_check(server_context_t* ctx, const http_message_t* request,
                      const char* action) {
  (void)ctx;
  (void)request;
  (void)action;
  return 0;
}
